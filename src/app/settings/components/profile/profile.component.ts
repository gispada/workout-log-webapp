import { Component } from '@angular/core'

@Component({
  selector: 'settings-profile',
  template: `<app-card cardTitle="Profile">Data</app-card>`
})
export class ProfileComponent {
  constructor() {}
}
